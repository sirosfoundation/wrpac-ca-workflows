name: plan
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
  pull-requests: write
jobs:
  plan:
    uses: sirosfoundation/wrpac-ca-workflows/.github/workflows/plan.yml@main
    with:
      state-repo: __STATE_REPO__
